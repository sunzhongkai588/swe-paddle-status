# SWE-Paddle 全量状态：105 条完整任务包

截至 2026-09-09，固定快照 [`207998dab8c4c76c8558c3cca8ef177c69f10c1c`](https://github.com/PaddlePaddle/community/tree/207998dab8c4c76c8558c3cca8ef177c69f10c1c/swe-paddle) 的 **105 条完整任务包**按目前已有证据汇总如下。另有 4 条仅 proposal（56723、78220、79391、79657），不计入 105 条。

| 全量结果 | 数量 | 口径 |
| --- | ---: | --- |
| 验证通过 | **90** | 已有 F2P/P2P 通过记录，包含以下两种原包状态。 |
| 其中：原包无必改 | **73** | 55 条沿用历史原包通过记录，18 条来自最新复验。 |
| 其中：通过但原包需改 | **17** | 14 条沿用历史记录，3 条来自最新复验。 |
| 验证未通过 | **8** | 已知测试、补丁或完整配对存在问题，不能判通过。 |
| 验证未完成 | **7** | 所需环境、硬件或完整重复验证尚未补齐。 |

关系为 **105 = 90 通过 + 8 未通过 + 7 未完成**；通过的 90 条内部又分为 **73 原包无必改 + 17 原包需改**。73 和 17 是 90 的子集，不能再次与 90 相加。

## 整体状态与证据来源分开看

首页展示的是当前 **105 条整体状态**，不是只展示最近新增或修改的 28 条。任务状态和记录来源是两个维度：未修改的任务继续沿用已有结论，不因本轮未重跑而降级为“待验证”。

| 记录来源 | 原包无必改 | 通过但原包需改 | 未通过 | 未完成 | 合计 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 沿用历史记录 | 55 | 14 | 3 | 5 | 77 |
| 最新验证记录 | 18 | 3 | 5 | 2 | 28 |
| 全量 | **73** | **17** | **8** | **7** | **105** |

最近一轮覆盖 9 条新增、19 条修改；其余 77 条任务包内容未变，本次没有重跑。历史 55 条原包通过记录保持通过，**不因未重跑降级，也不自动升级为新版正式评测资格**。本次网页调整是将已有证据合并成全量视图，没有新执行测试或修改测试断言。

通过的 90 条按证据来源分为：

- **8 条最新严格证据**：5 条所需 Python 源码一致且不要求原生编译，3 条有对应源码的原生运行时证据。
- **13 条最新兼容证据**：10 条原包无必改，3 条原包需改；尚不能视作指定 Base 原生运行时完全匹配。
- **69 条沿用通过记录**：55 条历史原包通过，14 条历史核心通过但原包需改；本次未升级这些记录的证据级别。

**73 条表示目前“原包无必改”，不是新增 73 条正式资格，也不代表 73 条全有新版严格运行时证据。** 其中包含 10 条兼容验证。正式模型评测、完整题面审查和新版流程资格仍需按相应门禁处理；本页不产生模型分数或资格放行。

## 32 条需要处理的任务

以下列全量 17 条“通过但原包需改”、8 条“未通过”和 7 条“未完成”。“历史”表示沿用旧记录；“最新”表示本轮验证记录。历史记录没有独立 F2P/P2P 数字字段，页面保留原始文字矩阵而不反推数字；数字留空不改变已有定性结论。

### 通过但原包需改：17 条

| Task | 来源 | 需要处理的问题 |
| --- | --- | --- |
| 41202 | 最新 | 受控入口 2 F2P / 1 P2P 成立；修正原入口的 python.paddle 导入与生成 proto 布局。 |
| 52948 | 历史 | 原运行布局会误导入源码包；固定从仓库根目录启动并核对实际加载的安装产物。 |
| 57827 | 历史 | 原脚本整文件收集 helper 和无关失败；明确已验证的目标与回归节点并复验。 |
| 59021 | 历史 | 原始脚本触发无关崩溃，另有静态初始化问题；明确节点、模式和回归执行顺序。 |
| 59127 | 最新 | 受控入口 84 F2P / 42 P2P 成立；修正 white_list 模块搜索路径。 |
| 59348 | 历史 | 原入口因 op_test 导入停止；补齐搜索路径与执行模式初始化后复验原包。 |
| 59374 | 历史 | 原选择器缺有效 P2P；把已验证的独立回归节点加入原入口，CUDA skip 不计 P2P。 |
| 59383 | 最新 | 分文件受控执行 28 F2P / 680 P2P 成立；修原入口收集方式，16 个 CPU skip 不计通过。 |
| 72800 | 历史 | 原入口两态均因搜索路径返回错误；补齐 legacy-test 路径后复验。 |
| 73125 | 历史 | 原脚本两态均未正常收集；修导入路径并增加非空收集和退出码检查。 |
| 73385 | 历史 | 原入口存在收集失败与选择器范围问题；固定路径和节点，缺失节点必须报错。 |
| 73387 | 历史 | 原入口两态均收集失败；将必要的仓库及 legacy-test 路径写入脚本。 |
| 73569 | 历史 | 原脚本无法导入 op_test；把 legacy-test 路径落实到原入口并双轮复验。 |
| 74184 | 历史 | 原入口收集错误且回归选择不完整；明确纳入已验证 P2P，避免空选择假绿。 |
| 78932 | 历史 | alias 测试只检查 truthiness；改为逐对对象身份断言并复验。 |
| 79197 | 历史 | 原脚本两态均无法导入 op_test；补齐搜索路径并检查实际收集。 |
| 79276 | 历史 | 原生配对已有证据，但原脚本缺 legacy-test 路径；修入口后复验。 |

### 验证未通过：8 条

| Task | 来源 | 未通过原因及下一步 |
| --- | --- | --- |
| 57741 | 历史 | CUDA 选择器类名拼写错误导致空收集；即使修拼写，唯一 CPU 节点仍没有 F2P。修选择器并增加真正触发原缺陷的目标。 |
| 58323 | 最新 | Base 缺 41 个目标节点的有效收集，Gold 仍受静态模式污染，原入口缺路径。修顶层 API 访问、模式隔离和入口。 |
| 59973 | 最新 | 47 F2P / 23 P2P 只是有效子集，两个静态回归在 Gold 仍失败。修初始化；skip 和空跑不计回归。 |
| 64519 | 最新 | Gold 仍受状态污染，另外存在静态回归失败与 6 个 ABI 无效 setup。既需修测试，也需匹配环境；13/2 只是有效子集。 |
| 73122 | 最新 | 指定原生运行时中 F2P 为 0，Base/Gold 都绿；测试必须真正触发原缺陷。 |
| 76259 | 历史 | 测试补丁已修，但实现补丁仍无法应用到声明的 Base，属于已知包问题；重新生成后还需 Windows / MSVC 验证。 |
| 76873 | 最新 | 原混合套件无法完整收集，CINN 覆盖缺失，SELU Gold 仍数值失败。9/14 仅为拆分 legacy 子集，不能当整包通过。 |
| 77495 | 历史 | 实现补丁来自较晚 preimage 且缺主功能链，无法形成有效 Gold；从声明 Base 重新生成完整补丁。 |

### 验证未完成：7 条

| Task | 来源 | 尚缺的验证 |
| --- | --- | --- |
| 59847 | 历史 | CPU 子集已有 36 F2P / 5 P2P，但任务必需 GPU，32 个 CUDA 节点未执行；需补齐 GPU 验证。 |
| 64320 | 历史 | CPU 子集已有 6 F2P / 26 P2P，但必需 GPU 未验证；还需纠正一个未调用目标 API 的护栏角色。 |
| 64881 | 最新 | 全量回归在替代环境的 Base/Gold 双轮均崩溃，并有旧 IR／新 PIR 类型冲突；需匹配原生运行环境。 |
| 74221 | 历史 | 仅有受控单轮配对，原入口有导入问题；修入口并补齐双轮验证。 |
| 74305 | 历史 | 仅有受控单轮配对，完整双轮尚未完成；修收集入口后复验。 |
| 78823 | 历史 | 只有 CPU 诊断，缺任务必需的 CUDA / XPU 目标配对证据；需加速器环境验证。 |
| 79386 | 最新 | 原生编译在 pybind 阶段超时，尚未完成有效 F2P/P2P 配对；需完成真实 Base/Gold 构建。 |

原历史数据把“未通过”和“未完成”合并在一个 20 条桶内。保留下来的 8 条原桶记录，本页依据原文显式拆为 **3 条未通过 + 5 条未完成**。其中 74221、74305、78823 原字段已标记 pending；59847、64320 虽未设置 pending 字段，但原矩阵明确写“必需 GPU 未验证”，原因也明确必需硬件未执行，因此归入未完成。这个拆分没有新增通过项，不是一次新跑，也不是豁免断言。76259 虽还需 Windows 资源，但有补丁不可应用的明确问题，仍归未通过。

## 数据与来源

整体数据由 [overall-status.js](assets/overall-status.js) 在浏览器中纯派生：

1. 读取历史 [data.js](assets/data.js) 的 96 条互斥完整记录（55 原包通过、21 原包需改、20 未通过或未完成）。
2. 以 [validation-update.js](assets/validation-update.js) 的最新 28 条记录替换 19 条修改项、加入 9 条新增项；其他 77 条保留原结论。
3. 按 105 条逐题状态实际计数，不通过“未登记问题即通过”的默认规则推断；缺记录、重复 ID、未知状态或来源不匹配直接报错。

14 条沿用的原包需改项，以及 8 条沿用的原失败桶记录，其原因、动作和文字矩阵在派生数据中逐字保留。历史 55 条通过项没有独立数值字段，所以只保留定性通过，不伪造逐节点数量。记录日期是核对更新日期，不把本次页面更新时间冒充历史测试执行时间。

冻结输入 SHA-256：

| 文件 | SHA-256 |
| --- | --- |
| data.js | `e70903974cabc3e483b23e903332cc050f2e504ae92354acbc55e5282a0539ec` |
| validation-update.js | `210090c36c031003e865580cf790d97de4852fbf8b4ea4875737370cae190415` |

原数据、[2026-09-09 增量报告](validation-update-20260909.md)及其已发布校验清单保持字节不变；增量报告作为更新日志保留，不再代替全量首页口径。本次仅发布公开字段派生视图，不包含私有执行日志、环境产物或解题补丁。
