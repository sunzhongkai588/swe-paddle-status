# SWE-Paddle 可评测性雷达

SWE-Paddle 社区任务的 F2P/P2P 验证状态看板。2026-09-09 页面更新使用快照
`207998dab8c4c76c8558c3cca8ef177c69f10c1c`：105 条完整任务包，另有 4 条
proposal-only 目录，不计入完整任务分母。

首页展示 **105 条任务各自最近一次有效记录组成的全量状态**，不是只展示最近新增的任务。
更新过的任务采用新记录；七文件未变的任务沿用已有结论，同时保留记录来源、
对应快照和记录更新日。“本次没有重跑”不等于“没有通过”，也不是一种验证状态。

| 全量当前结论 | 数量 | 含义 |
| --- | ---: | --- |
| F2P/P2P 通过，原包无必改 | 73 | 沿用 55 条已有两层通过记录，加上最近验证的 18 条 |
| F2P/P2P 通过，任务包需修改 | 17 | 沿用 14 条已有待修记录，加上最近验证的 3 条；问题不只包括入口 |
| 验证未通过 | 8 | 已确认存在 F2P、Gold 或任务包构建问题 |
| 待验证／验证未完成 | 7 | 缺完整对照、必要运行环境，或编译尚未完成；不冒充语义失败 |

**通过合计 90 条＝73 条原包无必改＋17 条通过但需修改**，另有 8 条未通过、
7 条待验证／未完成，合计 105 条。统计按 Task ID 去重，以最新有效记录覆盖旧记录，
不把新增结果再次累加到已有任务上。

验证结论与证据层级分别展示：90 条通过中，8 条有最近核对的严格源码／原生证据、
13 条有兼容环境诊断证据、69 条沿用既有通过记录。沿用记录不会自动升级为新的
严格运行时资格，兼容诊断也不伪装为 exact native 验证。因此 73 条“原包无必改”
不等于新授予 73 条正式评测资格。本次没有重跑样本或新增模型成绩。

完整口径与问题清单见[全量状态报告](docs/overall-status-20260909.md)。
最近的 9 条新增＋19 条修改（28 条）仅作为更新记录，见
[增量验证报告](docs/validation-update-20260909.md)；另保留
[2026-09-07 统计更正依据](docs/validation-correction-20260907.md)。

## 本地预览

```bash
cd docs
python3 -m http.server 4173
```

浏览器访问 <http://127.0.0.1:4173/>。

## 发布到 GitHub Pages

1. 将本目录推送到一个 GitHub 仓库的 `main` 分支。
2. 打开仓库的 **Settings → Pages**。
3. 在 **Build and deployment** 中选择 **Deploy from a branch**。
4. Branch 选择 `main`，目录选择 `/docs`，保存。

之后每次更新 `main/docs`，GitHub Pages 都会自动重新发布。

## 数据维护

- 页面结构：`docs/index.html`
- 样式：`docs/assets/styles.css`
- 交互：`docs/assets/app.js`
- 历史任务与结论：`docs/assets/data.js`（保留原快照，不覆盖历史证据）
- 最近一次增量公开投影：`docs/assets/validation-update.js`（保留已发布字节）
- 全量最新有效记录汇总：`docs/assets/overall-status.js`
- 全量口径公开报告：`docs/overall-status-20260909.md`
- 当前公开页面与数据校验清单：`docs/overall-status-20260909.sha256`

最近 28 条按显式 ID 更新，其余 77 条沿用已有记录，所有 105 条都进入全量状态
统计和筛选。验证状态与来源正交，缺少记录不能默认为通过。
原有合并桶中的 59847、64320 明确缺少必要 GPU 验证，本页将其归入“未完成”，
不是重新测试后判失败或通过。失败与未完成按显式证据分类，不依赖模糊文本猜测。

旧的 `validation-update-20260909.sha256` 对应已发布提交 `9e5675d` 的页面，
原样保留；其中旧页面资源的哈希不能用于核对改版后的页面。当前发布采用新的
`overall-status-20260909.sha256`，不覆盖旧发布清单。

公开投影仅包含任务元信息、状态、F2P/P2P 数量、证据层级、原因、建议和
封存源报告的 SHA-256。不得直接复制私有原始报告：它可能包含本地路径、
构建环境或其他不应公开的字段。原始测试日志、参考解答和模型轨迹不发布到本站。

## 验证页面

```bash
node --test tests/*.test.cjs
```

可设置 `SWE_PADDLE_SEALED_RESULT` 指向本地封存报告，启用逐题源数据核对；
未设置时该项明确跳过。不要把私有源报告提交到本站仓库。

真实浏览器检查使用 `node tests/browser-smoke.cjs`，由外部测试环境提供
Playwright、浏览器和中文字体，不向本站引入生产依赖。默认启动并关闭临时本地
服务器；设置 `SWE_PADDLE_STATUS_URL` 可复验线上页面，设置
`SWE_PADDLE_SCREENSHOT_DIR` 可将截图保存在仓库以外。

在 `docs` 目录运行 `sha256sum -c overall-status-20260909.sha256` 可核对当前
公开数据、报告与页面资源。后续发布应新增校验清单并保留此前版本。

发布前还需检查浏览器控制台、状态筛选、任务详情、移动布局和资源加载。
推送后核对 GitHub Pages 构建对应的提交以及线上文件内容，不能把“已推送”
直接当成“网页已更新”。
